<div class="card-header h3">
  Edit <%= @package.name %>
</div>
<div class="card body container-fluid">

  <%= render 'form', package: @package %>

  <p><%= link_to "Back to Package", event_package_path(@event), class: "btn btn-primary"  %></p>
</div>
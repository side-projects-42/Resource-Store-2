# frozen_string_literal: true

# By default, new gems create a folder, with the same name as the project.
# Inside that folder is a `version.rb` folder that will contain the version number.
# The version number will live inside the `version.rb` in a Module with the same name as the project.
module Candidates
  VERSION = "0.5.0"
end

const divide = () => 2000 / 100;

const square = (number) => number * number;

const add = (num1, num2) => num1 + num2;

function zomg(increment) {
  let x = 1;

  if (increment) {
    x++;
  }

  console.log(x);
}

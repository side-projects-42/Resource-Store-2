import _classCallCheck from "@babel/runtime/helpers/esm/classCallCheck";
import _createClass from "@babel/runtime/helpers/esm/createClass";

var BaseMetricsAsyncCache = /*#__PURE__*/function () {
  function BaseMetricsAsyncCache() {
    _classCallCheck(this, BaseMetricsAsyncCache);
  }

  _createClass(BaseMetricsAsyncCache, [{
    key: "isEmpty",
    value:
    /**
     * Async storage caches will always count as empty from an SDK Producer point of view.
     */
    function isEmpty() {
      return true;
    }
    /**
     * On async storages we will save metrics on Redis but nothing more. No clean ups or consumptions.
     */

  }, {
    key: "clear",
    value: function clear() {// noop.
    }
  }]);

  return BaseMetricsAsyncCache;
}();

export { BaseMetricsAsyncCache as default };